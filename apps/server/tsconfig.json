{
  "extends": "../../tsconfig.app.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "ts-node": {
    // it is here so ts-node can catch up globally defined types in "files" section below
    "files": true
  },
  "files": [
    "src/main.ts",
    "src/index.d.ts"
  ]
}
